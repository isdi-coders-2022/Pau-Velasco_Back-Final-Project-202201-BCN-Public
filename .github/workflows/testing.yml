name: Testing on push

on: push
env:
  JWT_SECRET: CC83733CB0AF8B884FF6577086B87909

jobs:
  test:
    name: Test passes
    runs-on: ubuntu-latest

    steps:
      - name: Git checkout
        uses: actions/checkout@v2

      - name: Install modules
        run: npm ci

      - name: Testing
        run: npm test
